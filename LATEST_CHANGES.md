# Latest changes

更新时间：2026-09-07

功能基线提交：`409ae95`（本交接机制在其后的提交中加入）

详细交接文档：[`docs/0907_认知雷达拆分与更新规则说明.md`](docs/0907_认知雷达拆分与更新规则说明.md)

## 今天完成的内容

- `/decompose` 顶层增加 `themes`，直接返回精炼管理主题；标题由 Schema 强制限制为 2—6 个字符。
- 拆分与更新新增 `rank_documents` 材料整理阶段，逐篇评估相关性、决策价值、直接性、公司适用性、时效性和证据角色；只剔除确实无关的材料，保留支持、反驳与限定材料。
- 拆分结果是管理主题，不是最高分文献。检索材料决定主题内容，领导视角负责把材料转换为决策问题并排序。
- 配置加入长鑫存储默认组织背景，以及新技术、市场、政策、竞争对象、经营问题五类 Topic 的差异化关注项。
- 最终结构以有证据主题为主体，通常最多保留两个关键 `gap`；全部缺少证据时返回 `insufficient_input`。
- 多来源更新按直接性、公司适用性、方法透明度、时效性和来源独立性判断，不按文献数量投票。
- 内部材料侧重公司经营事实；外部一手材料侧重市场、竞争、技术和政策事实。
- `source_policy` 已进入拆分、认知增量提取、更新计划和最终审查阶段。
- 所有改动文件和位置已经记录在 0907 交接文档中。

## 接手时先执行

```powershell
git status --short --branch
git pull --ff-only origin main
Get-Content -LiteralPath LATEST_CHANGES.md -Encoding UTF8
```

如果工作区不干净，先查看并保留本机修改，不要直接拉取或覆盖。

## 验证状态

- 最近一次完整测试：`.venv\Scripts\python.exe -m pytest -q`
- 结果：31 项通过。
- 离线演示：`.venv\Scripts\python.exe -m radar.demo` 已通过。

## 下一步建议

- 使用脱敏的长鑫真实 Topic 和检索文献验证 `themes` 是否稳定保持在 4—6 个模块、每个名称不超过 6 个字符，并由业务负责人评审主题是否真正影响领导判断。
- 根据真实材料补齐文献元信息字段约定，例如发布时间、发布机构、文献类型、适用产品、统计口径和 `family_id`。
- 对超出单次模型上下文的文献列表增加分批证据提取和相关证据召回。
